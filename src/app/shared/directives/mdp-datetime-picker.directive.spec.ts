/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';

import { ElementRef } from '@angular/core';

import { MdpDatetimePickerDirective } from './mdp-datetime-picker.directive';

describe('MdpDatetimePickerDirective', () => {

  it('should create an instance', () => {
    // TODO: fix this test using: https://angular.io/docs/ts/latest/guide/testing.html#!#attribute-directive
    // if we still use the jQuery datetime picker instead of ng2-datetimepicker
    //let el = new ElementRef({});
    //let directive = new MdpDatetimePickerDirective(el);
    //expect(directive).toBeTruthy();
  });

});
